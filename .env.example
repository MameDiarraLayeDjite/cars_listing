# Database configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=car_sales

# JWT secret key (keep this secure and secret)
JWT_SECRET=your_jwt_secret_key

# Server port
PORT=5000

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000
